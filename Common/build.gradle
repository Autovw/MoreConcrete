plugins {
    id 'idea'
    id 'java'
    id 'net.neoforged.moddev'
}

group = 'com.autovw.moreconcrete'
version = "${mod_version}-${minecraft_version}"
base.archivesName = 'moreconcrete-common'

neoForge {
    neoFormVersion = neo_form_version
}

dependencies {
}
